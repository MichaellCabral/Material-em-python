C = float(input('Temperatura em Celsius: '))
F = ((9 * C) / 5 ) + 32 # a ordem de prescedência é a ordem que os operadores aparecem (não precisa de parenteses)
print(f'A temperatura de {C} corresponde a {F}')

# Converter temperatura em C° para F°